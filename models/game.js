const DB = require('./database');

class Game {
    constructor(basicItem) {

        console.log(basicItem)
        // todo: take raw item and try to find it in game database
        //       if it's not exists - create new a record.
    }

    loadData(){
        // todo: how?
    }
}

module.exports = Game